prek>=0.3.0,<0.5  # For pre-commit hooks; lowercap to hook requirements
pydata-sphinx-theme>=0.18.0,<0.21  # Theme; lowercap to fixed right sidebar
sphinx>=8,<10  # Docs generator; cap to range confirmed supported by deps
sphinx-design>=0.6.0,<0.8  # For dropdowns & panels; lowercap to modern Python
sphinxcontrib-video>=0.4.0,<0.5  # Video directive with controls, autoplay, etc
